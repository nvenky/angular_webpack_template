# Webpack - AngularJS template

Template for getting started with Webpack and AngularJS.

Also added dependencies to:
* CoffeeScript
* SASS
* Bootstrap
* FontAwesome

## Build

Run the following command:
```ssh
npm install webpack -g
webpack --progress --display-error-details
```
